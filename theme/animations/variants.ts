import { accent } from "theme/colors";

export const iconVariants = {
  hover: {},
  tap: {}
};

export const pathVariants = {
  hover: { fill: accent, duration: 0.3 },
  tap: { fill: accent, duration: 0.1 }
};
